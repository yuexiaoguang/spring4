/**
 * Spring重新打包<a href="http://asm.ow2.org">ASM</a> (仅供内部使用).
 *
 * <p>这种重新打包技术可以避免在应用程序级别或第三方库和框架上与ASM的依赖发生任何潜在冲突.
 *
 * <p>由于此重新打包发生在类文件级别, 因此源码和javadoc在此处不可用.
 */
package org.springframework.asm;
